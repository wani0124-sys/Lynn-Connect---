# 세션 쿠키 서명 시크릿 (운영에서는 반드시 설정)
SESSION_SECRET=

# Supabase (서버 전용. 브라우저에서 직접 호출하지 않으므로 anon key는 두지 않는다)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Claude API (서버 전용). 현장 점검결과 AI 분석 기능에서 사용. console.anthropic.com에서 발급.
ANTHROPIC_API_KEY=
